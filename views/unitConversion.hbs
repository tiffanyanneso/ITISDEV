<!DOCTYPE html>
<html>
    <head>
        <title> Unit Converter </title>
        <meta name="description" content="Unit Converter">
        <meta name="keyword" content="View Purchase">
        <link rel="stylesheet" type="text/css" href="../css/general.css">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    </head>

    <body>
        <label for="unitA">Unit A</label> <input type="text" id="unitA"> <br>
        <label for="unitB">Unit B</label> <input type="text" id="unitB"> <br>
        <label for="ratio">Ratio</label> <input type="number" id="ratio"> <br>
        <label for="operator">Operator</label> <input type="text" id="operator"><br>
        <button id="save-unit" class="btn-light-blue">Save</button>

        <script>
            $(document).ready(function() {

                //should only be either multiplcation or division
                function checkOperator(operator) {

                }

                function checkSameUnit (unitA, unitB) {
                    if (unitA == unitB)
                        return true;
                    else
                        return false;
                }

                $('#save-unit').click(function(){
                    var unitA = $('#unitA').val();
                    var unitB = $('#unitB').val();
                    var ratio = $('#ratio').val();
                    var operator = $('#operator').val();

                    $.post('/saveUnit', {unitA, unitB, ratio, operator});

                    $('#unitA').val('');
                    $('#unitB').val('');
                    $('#ratio').val('');
                    $('#operator').val('');
                })


            })
        </script>
    </body>

</html>